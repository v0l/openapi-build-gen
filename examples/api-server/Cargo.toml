[package]
name = "api-server-example"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }

[build-dependencies]
openapi-build-gen = { path = "../.." }
